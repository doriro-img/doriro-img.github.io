# 블로그(티스토리) 작성 규격 — heelovee.tistory.com

운영 중인 블로그: **https://heelovee.tistory.com** (이름: Doriro · 글 79편 · 누적 방문 11.9만)
글 79편을 전량 정독해 정리한 규격. **새 글은 반드시 이 형식을 따른다.**

> **말투는 이 파일이 아니라 [VOICE.md](VOICE.md)를 본다.**
> 이 파일은 구조와 배치를 다루고 VOICE.md가 목소리를 다룬다. **둘 다 읽어야 한다.**

**글 주제는 이 서비스에 한정되지 않는다.** 요리·맛집·여행·생활정보가 주력이고 목적은 검색 유입이다.

카테고리는 `일상·취미`(요리·레시피 / 생활·여행·TIP) 와 `돈되는 정보` 두 갈래다.
실제 분포는 요리가 가장 많고 그다음이 김포 근처 맛집이다.

## 왜 형식을 맞춰야 하나

1. 이 블로그는 **h2/h3 태그 기반 자동 목차 플러그인**을 쓴다.
   기호(`●`, `■`)로 소제목을 만들면 **목차에 안 잡힌다.** 반드시 헤딩 태그를 쓸 것.
2. 기존 글과 톤이 다르면 같은 사람이 쓴 것으로 안 읽힌다.

## 키워드 — 원고 쓰기 전에 먼저 잡는다 (필수)

**원고부터 쓰지 않는다. 키워드 리서치를 먼저 하고 그 결과로 제목·소제목·태그를 확정한 뒤 쓴다.** 검색량·검색의도를 데이터로 확인하지 않고 감으로 쓰면 아무리 잘 써도 유입이 안 붙는다.

### 절차 (순서대로)
1. **핵심 키워드 + 롱테일 후보**를 웹 검색으로 조사한다. 사람들이 실제 입력하는 형태를 찾는다
2. **자동완성·연관검색어**를 확인해 "같이 치는 말"을 확보한다 (예: "벌초 시기" → "벌초 시기 2026", "벌초 대행 비용", "벌초 안 하면")
3. **상위 노출 글 제목 패턴**을 본다. 경쟁이 세면 더 구체적인 롱테일로 각도를 튼다
4. 확보한 키워드로 **제목·소제목(h2/h3)·태그·끝 검색용 섹션**을 짠다 → 그다음 원고

### 롱테일 우선
- 신규·저트래픽 블로그는 검색량 큰 대표 키워드(예: "전기요금")로는 상위 노출이 거의 불가능하다
- **경쟁 낮고 의도 뚜렷한 롱테일**(예: "에어컨 하루 8시간 전기세")을 노린다. 검색량은 작아도 실제로 잡힌다
- 제목에 대표 키워드 + 롱테일을 함께 담는다

### 한계 (정직하게)
- 네이버 **키워드도구의 정확한 월 검색량**은 검색광고 로그인이 필요해 여기서 못 뽑는다 → 그 숫자는 사용자가 크로스체크
- 웹 검색으로 하는 건 연관검색어·자동완성·경쟁 파악까지다. 정확한 수치가 아니라 **방향**을 잡는 용도
- 원고 하단 메모에 "노린 키워드 / 롱테일 / 확신도"를 남겨 나중에 검증 가능하게 한다

---

## 제목

키워드를 나열하고 마침표로 끊는 SEO형. 검색어를 그대로 담는다.

```
소고기무국 맛있게 하는법. 소고기뭇국 간편 레시피로 진한 국물!
김포 통진읍 맛집 '쭈꾸미일당백 김포점' 매장/포장 가격. 쭈꾸미 효능
강아지 눈세정제, 눈물클리너 추천! 눈물 냄새, 눈물자국 관리 (내돈내산)
대만 여행 담배, 전자담배 반입 가능? 면세 한도 알아보기
```

`핵심 키워드 + 부가 키워드. 부연 설명` 구조다. 한 문장으로 끝내지 않는다.

- 물음표와 느낌표를 그대로 쓴다 (`칼로리는?` `추천!`)
- 상호명은 **작은따옴표**로 감싼다 (`'다하누촌 김포본점'`)
- 협찬이 아니면 `(내돈내산)` 을 붙인다
- 맛집 글은 앞에 **지역명**을 박는다 (`김포 구래동 맛집` `홍대 연남동`)

## 본문 구조

```
p    글 제목을 그대로 한 번 더 (첫 줄)
h2   도입 — 문제 제기        → 개인 경험 한두 문단 + 결과 이미지
h2   왜 이런 일이 생기나 / 배경
h2   메인 (글 제목과 같거나 거의 같게)
h3   1. 단계 이름     → 짧은 설명 p + 이미지
h3   2. 단계 이름     → 짧은 설명 p + 이미지
h3   3. 단계 이름     → ...
h2   마무리 단락
p    "긴 글 읽어주셔서 감사합니다"
```

**h4는 쓰지 않는다.** 목차 플러그인이 `h2 < h3 < h4` 순으로 들여쓰므로,
글 앞부분에 h4를 두면 부모도 없이 제일 깊게 밀려 목차가 망가져 보인다.
**큰 단락은 전부 h2, 그 아래 단계만 h3** 로 두 단계만 쓴다.

### 소제목은 질문형을 섞는다

옛 글의 가장 큰 특징이다. 정보형만 쭉 늘어놓으면 이 블로그 글로 안 읽힌다.

```
비용은 얼마?              받아보니 명품과 똑같나?
추가금 있다구요?          18,800 원주고 400mL를 살만한가?
오마카세란?               6박 7일 환전은 얼마?
```

### 끝에 검색용 섹션을 붙인다

요리·맛집·제품 글은 거의 예외 없이 마지막에 이런 h2가 붙는다. 검색 유입용이다.

```
OO 효능        OO 칼로리       OO 가격       찾아가시는길
```

## 톤

→ **[VOICE.md](VOICE.md)** 를 본다. 어미 빈도와 고유 어휘와 금지 표현이 전부 거기 있다.

여기서 딱 하나만 옮기면 **`~더라고요`가 두 번은 나와야 한다.** 그게 이 사람 목소리다.

## 이미지 배치

**글만 길면 안 읽고 이미지만 많아도 안 읽는다. 배치가 전부다.**

### 몇 장 넣나

실측값이다. 77편을 세어보니 이렇다.

| 글 종류 | 이미지 | 비고 |
|---|---|---|
| 요리·레시피 | **9~14장** | 과정을 단계마다 찍는다 |
| 맛집·카페 | **5~16장** | 음식 위주. 30장짜리도 있다 |
| 여행 | **15~30장** | 가장 많다 |
| 제품 리뷰 | **5~12장** | 개봉·사용·전후 비교 |
| 정보·방법 안내 | **4~9장** | 화면 캡처 위주 |

**최소 5장이다.** 그 밑이면 글이 허전해서 이탈한다.

### 어디에 넣나

- **설명 문장 바로 아래.** 문단 위나 소제목 바로 밑이 아니다
- **h3 단계마다 최소 한 장.** 단계를 글로만 설명하지 않는다
- **도입부에 결과부터 보여준다.** 완성 사진이나 결과 화면을 먼저 던진다
- 텍스트가 **연속 3문단**을 넘으면 이미지를 하나 끼운다
- 이미지를 **두 장 이상 연달아** 붙이지 않는다. 사이에 한 줄이라도 설명을 넣는다
  (예외: 예시를 몰아 보여주는 구간)

### 만들 때

- **모든 `<img>`에 `style="max-width:100%;height:auto;"` 를 반드시 붙인다.**
  HTML 모드로 넣은 이미지는 에디터가 감싸주지 않아서 없으면 원본 크기(2000px+)로 본문을 뚫고 나간다
- `alt` 를 반드시 채운다. 검색 유입에 도움이 된다
- **전체 페이지 캡처를 쓰지 않는다.** 세로가 길면 본문 폭에 맞췄을 때 글씨가 안 보인다
- 가로 폭은 860~1100px. 본문 폭이 약 700px이라 그 이상은 낭비다

### 도식 스타일 — 표만 반복하지 않는다

**매 이미지를 표·박스·그리드로만 채우면 심심하고 딱딱해 보인다.** 표를 금지하는 게 아니라, 표만 반복하지 말라는 것이다. 요리·강아지 나오는 이 블로그의 친근한 톤과 안 붙는다.

**더 중요한 것 — 도식에 글씨만 쭉 늘어놓지 않는다.** 이미지인데 안에 텍스트만 가득하면, 독자는 "이미지 없이 텍스트만 계속된다"고 느껴 싫어한다. 애써 이미지를 넣은 효과가 사라진다. 그래서 **모든 도식은 한눈에 '그림'으로 읽혀야 한다.**

- **글자 대 시각요소 비율**을 의식한다. 그림·색 블록·도형·그래프·아이콘 같은 시각 요소가 도식 면적의 상당 부분을 차지하게 한다. 글씨만으로 채우지 않는다
- 순수 표조차도 색 헤더·강조 색·구분선으로 '표처럼 보이는 그래픽'이 되게 한다. 흰 바탕에 검은 글씨만 늘어놓지 않는다
- **진짜 표가 맞는 것**(요금표·단가표처럼 행·열 데이터가 핵심)은 그대로 표로 간다
- **그 외**는 일러스트를 섞는다: 실제 사물을 SVG로 그리고(에어컨·집·동전·온도계·해 등), 옅은 톤 배경·둥근 모서리·부드러운 그림자로. 에너지 기둥·게이지·아이콘·장면(scene)형 배치를 쓴다
- **한 글 안에서 스타일을 섞는다.** 5장이면 표 1~2장 + 일러스트·그래프 여러 장으로. 같은 표 틀 5장은 지양
- 좋은 예: `art/ac03b-illust.html`(에어컨 그림+찬바람+둥근 기둥), `art/ac05b-illust.html`(집+동전 스택). 표가 맞는 예: `art/ac01-label.html`(에너지효율 라벨 목업), 요금표
- 어떤 이미지를 표로/일러스트로 할지는 매번 물어보지 말고 적절히 알아서 배분한다

## 가독성

정렬과 여백에 기준이 없으면 글이 벽처럼 보인다.

- **문단은 1~3문장.** 네 문장을 넘기면 끊는다
- 문단 사이는 `<p>` 로 분리한다. `<br />` 두 개로 때우지 않는다
- **정렬은 왼쪽 정렬 그대로 둔다.** 가운데 정렬이나 양쪽 정렬을 쓰지 않는다
  (모바일에서 줄 간격이 깨지고 읽는 속도가 떨어진다)
- 숫자·가격·조건이 세 개를 넘으면 **`<ul>` 이나 표**로 뺀다. 문장에 우겨넣지 않는다
- 강조는 `<b>` 로 한다. 한 섹션에 한두 군데까지다
- 핵심 공식이나 결론은 `<blockquote>` 로 한 번 띄운다
- 링크는 문장 안에 숨기지 말고 **한 줄로 따로** 놓는다
- 숫자는 환산이나 단위를 붙인다 (`100,000TWD (4,171,000KRW)` `100g당 대략 200kcal`)

## 이미지 저장소 — 이 저장소가 곧 CDN이다

빌드가 없다. PNG를 넣고 푸시하면 그게 곧 배포다.

```
2026/08/파일.png   →   https://doriro-img.github.io/2026/08/파일.png
```

| 만드는 것 | 명령 |
|---|---|
| 설명 도식·비교표 | `art/이름.html` 작성 후 `node make-art.cjs` |
| 웹페이지 화면 컷 | `node make-shots.cjs` |
| 올리기 | `node push.cjs "설명"` |

**올해/이번달 폴더에 자동으로 저장된다.**
밖에서 만든 이미지는 그 폴더에 직접 넣고 푸시하면 된다.

### 규칙

- **파일명은 ASCII.** 한글은 주소에서 깨진다 (`push.cjs`가 올리기 전에 막는다)
- **지우거나 이름을 바꾸지 않는다.** 발행된 글이 이 주소를 직접 불러온다.
  깨져도 본인 브라우저 캐시 때문에 **독자한테만 보인다.** 수정은 같은 파일명 덮어쓰기로
- 도식은 폭 **860~1100px**, **밝은 배경**으로 만든다 (티스토리 스킨이 흰 배경)
  폰트는 `'Pretendard', 'Malgun Gothic', sans-serif`

## 대표이미지(썸네일) — 전용 카드를 따로 만든다

본문 이미지를 전부 외부 링크(`doriro-img.github.io`)로 넣으면, **티스토리 대표이미지 후보로 안 잡힌다.** 대표이미지 UI는 보통 티스토리에 직접 업로드된 이미지만 후보로 보여준다.

그래서 글마다 **썸네일 전용 카드 1장**을 만든다.

- 파일명 규칙: `art/tc-<주제>.html` → `tc-<주제>.png` (tc = thumbnail card)
- 크기: **800x800 정사각** (목록에서 정사각으로 크롭되는 스킨이 많다). 핵심 텍스트·그림은 가운데로
- 내용: 큰 제목(핵심 롱테일) + 관련 일러스트 + 소구 키워드 한 줄. 목록에서 눈에 띄어 클릭률에 기여
- 발행 시: 이 PNG를 저장 → 티스토리에 **직접 업로드** → 대표이미지로 지정. 본문엔 안 넣어도 된다(넣었다 줄만 지워도 대표이미지는 유지)
- 예: `art/tc-aircon.html`, `art/tc-beolcho.html`

## 원샷 복붙

티스토리 에디터 오른쪽 위 **[HTML]** 모드로 바꾸고 붙여넣으면
외부 이미지가 그대로 살아난다. **글과 이미지를 한 번에 붙여넣을 수 있다.**

**남는 수동 작업은 하나뿐이다 — 대표 이미지(썸네일) 지정.**

## 발행 전 체크

- [ ] **[VOICE.md](VOICE.md) 자가 점검을 통과했는가** (`~더라고요` · 혼잣말 · 쉼표 · 줄표)
- [ ] 이미지가 5장 이상이고 단계마다 붙었는가
- [ ] 모든 `<img>`에 `style="max-width:100%;height:auto;"` 와 `alt` 가 있는가
- [ ] 소제목에 질문형이 하나라도 있는가
- [ ] 끝에 `효능` `칼로리` `가격` `찾아가시는길` 같은 검색용 섹션을 붙였는가
- [ ] 마무리가 `긴 글 읽어주셔서 감사합니다` 인가
- [ ] 이미지를 푸시했는가 (안 하면 미리보기에서 깨진다)
- [ ] HTML 모드로 붙여넣었는가
- [ ] 대표 이미지 지정
- [ ] 태그 7~10개 (옛 글 실측 평균은 9개)
