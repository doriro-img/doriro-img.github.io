# 블로그(티스토리) 작성 규격 — heelovee.tistory.com

운영 중인 블로그: **https://heelovee.tistory.com** (이름: Doriro · 글 78편)
기존 글의 형식을 분석해 정리한 규격. **새 글은 반드시 이 형식을 따른다.**

**글 주제는 이 서비스에 한정되지 않는다.** 여행·생활·정보글이 원래 주력이고 목적은 검색 유입이다.

## 왜 형식을 맞춰야 하나

1. 이 블로그는 **h2/h3 태그 기반 자동 목차 플러그인**을 쓴다.
   기호(`●`, `■`)로 소제목을 만들면 **목차에 안 잡힌다.** 반드시 헤딩 태그를 쓸 것.
2. 기존 글과 톤이 다르면 같은 사람이 쓴 것으로 안 읽힌다.

## 제목

키워드를 나열하고 마침표로 끊는 SEO형. 검색어를 그대로 담는다.

```
대만 여행 담배, 전자담배 반입 가능? 면세 한도 알아보기
티스토리 블로그 자동 목차 만드는 방법. 그 어떤 방법보다 간단한 초보자용
```

→ `핵심 키워드 + 부가 키워드. 부연 설명` 구조. 한 문장으로 끝내지 않는다.

## 본문 구조

```
p    글 제목을 그대로 한 번 더 (첫 줄)
h2   도입 — 문제 제기        → 개인 경험 한두 문단 + 결과 이미지
h2   왜 이런 일이 생기나 / 배경
h2   메인 (글 제목과 같거나 거의 같게)
h3   1. 단계 이름     → 짧은 설명 p + 이미지
h3   2. 단계 이름     → 짧은 설명 p + 이미지
h3   3. 단계 이름     → ...
h2   마무리 단락
p    "긴 글 읽어주셔서 감사합니다"
```

**h4는 쓰지 않는다.** 목차 플러그인이 `h2 < h3 < h4` 순으로 들여쓰므로,
글 앞부분에 h4를 두면 부모도 없이 제일 깊게 밀려 목차가 망가져 보인다.
**큰 단락은 전부 h2, 그 아래 단계만 h3** 로 두 단계만 쓴다.

## 톤

- 친근한 존댓말. `~해요` `~인데요` `~하세요` 를 `~습니다`와 섞어 쓴다
- 개인 경험을 한 줄 넣는다 ("이번에 10월에 남편과 대만여행을 가기로 했어요")
- 독자를 가르치지 않는다. 같이 알아보는 톤
- 문단은 짧게. 두세 문장마다 끊는다

## 이미지

- 단계마다 한 장씩. 설명 문장 **바로 아래**에 넣는다
- 도입부에 결과 화면(예시)을 먼저 보여준다
- **모든 `<img>`에 `style="max-width:100%;height:auto;"` 를 반드시 붙인다.**
  HTML 모드로 넣은 이미지는 에디터가 감싸주지 않아서, 없으면 원본 크기(2000px+)로 본문을 뚫고 나간다
- **전체 페이지 캡처를 쓰지 않는다.** 세로가 길면 본문 폭에 맞췄을 때 글씨가 안 보인다

## 이미지 저장소 — `C:\dev\blog-cdn`

앱 저장소와 **분리돼 있다.** 빌드가 없고, PNG를 넣고 푸시하면 그게 곧 배포다.

```
C:\dev\blog-cdn\2026\08\파일.png   →   https://doriro-img.github.io/2026/08/파일.png
```

| 만드는 것 | 명령 |
|---|---|
| 앱 화면 컷 | `node make-shots.cjs` (dev 서버 켜둘 것) |
| 설명 도식·비교표 | `art/이름.html` 작성 후 `node make-art.cjs` |
| 올리기 | `node push.cjs "설명"` |

둘 다 **올해/이번달 폴더에 자동으로 저장된다.**

### 규칙

- **파일명은 ASCII.** 한글은 주소에서 깨진다 (`push.cjs`가 올리기 전에 막는다)
- **지우거나 이름을 바꾸지 않는다.** 발행된 글이 이 주소를 직접 불러온다.
  깨져도 본인 브라우저 캐시 때문에 **독자한테만 보인다.** 수정은 같은 파일명 덮어쓰기로
- 도식은 폭 **860~1100px**, **밝은 배경**으로 만든다 (티스토리 스킨이 흰 배경)
  폰트는 `'Pretendard', 'Malgun Gothic', sans-serif`

## 원샷 복붙

티스토리 에디터 오른쪽 위 **[HTML]** 모드로 바꾸고 붙여넣으면
외부 이미지가 그대로 살아난다. **글과 이미지를 한 번에 붙여넣을 수 있다.**

**남는 수동 작업은 하나뿐이다 — 대표 이미지(썸네일) 지정.**

## 발행 전 체크

- [ ] 이미지를 `blog-cdn`에 푸시했는가 (안 하면 미리보기에서 깨진다)
- [ ] HTML 모드로 붙여넣었는가
- [ ] 대표 이미지 지정
- [ ] 태그 6~8개
