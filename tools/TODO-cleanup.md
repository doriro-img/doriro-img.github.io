# 남은 작업 — 2026-08-23 기준

9월 세션이 아직 돌고 있습니다 (67/90편). **`posts/` 의 202609 와 `art/` 를 건드리지 마세요.**
공용 도구(`audit.cjs` `kw.cjs`)도 세션 도는 중에는 고치지 않습니다 — 그 세션이 매 배치마다 씁니다.

---

## 오늘 끝낸 것 (참고)

```
검색량 연동        tools/advol.cjs — 네이버 검색광고 keywordstool. 키는 .env
                  kw.cjs 가 자동으로 붙입니다 (--novol 로 끔)
                  저수요 · 대표층 누락 · 유입권 없음 경고
audit 소제목 검사   씨앗 빼고 꼬리말만 보던 것 → 롱테일 원형 2개 이상
                  8월 27편 통과 · 9월은 세션이 스스로 57편 교정
8월 소제목 교정     47줄 / 24편. 롱테일 원형 반영 5% → 57%
비밀키 가드        push.cjs 가 .env 값을 스테이징 파일과 대조해 차단
프롬프트           검색량 경고 대응 + 의도 우선 규칙 + 소제목 예시 교체
                  537 → 557줄. 원본은 같은 폴더 .bak
후보 풀            tools/candidates_202610.md — 88개 실측
```

---

## 1. 미푸시 커밋 4개

```bash
cd C:/dev/blog-cdn && git push origin main
```

원격 `8f8bdef` · 로컬 `88ecfb6`. 유출 파일은 사용자가 force push 로 정리 완료(404 확인).

---

## 2. 후보 풀 확대 — 10월 라인업 전에

지금 `candidates_202610.md` 는 88개인데 10월이 90편이라 딱 맞습니다. 여유가 없습니다.

씨앗을 30개 → 100개로 늘려 300개 이상 확보하면 **8·9월 하위권 교체도 선택지가 됩니다.**
지금은 풀이 좁아서 "8월에 쓰면 10월에 못 쓴다"라 교체가 이득이 아닙니다.

```
현재 씨앗 30개 → 연관 5,175개 → 필터 후 88개
```

`tools/advol.cjs` 의 `volume()` 이 돌려주는 `raw` 가 연관 키워드 전체입니다.
필터 기준은 `candidates_202610.md` 머리말에 적혀 있습니다.

**주의**: INFO 정규식이 3,138개(61%)를 죽였는데 그중 129개는 실제로 정보성이었습니다.
정규식을 넓히거나, 아예 볼륨·CTR 로만 거르고 의도 판정은 사람이 하세요.

---

## 3. 9월 종료 후 — 검수와 교정

시작 조건: `node audit.cjs 202609 --full` 이 `90편 점검 · 문제 0편`

```bash
node audit.cjs 202609 --full
node tools/artcheck.cjs --posts 202609
node tools/sync.cjs 202609 --check
node tools/deck.cjs 202609
```

### 주제 교체 1편

```
긴급복지지원제도    긴급복지 800회 · 유입권 키워드 0개 · 유입도 수익도 없음
                  87편 중 유일하게 새로 써야 하는 편
```

### 제목 교체 1편

```
KRX 금시장   현재 제목이 "KRX 금시장"(970회)을 잡고 있음
             KRX금현물 46,800 / 한국금거래소 1,967,000
             씨앗이 틀린 것이지 주제가 나쁜 게 아님
```

### 키워드 전수 검증

8월과 같은 절차. `kw_202609.json` 은 8/22 수집분이라 검색량이 없습니다.
`advol.cjs` 로 상위 키워드 + 씨앗 조각을 조회해서 제목이 최대 검색량을 잡았는지 확인합니다.

**폐기 판정 전에 반드시 씨앗 조각까지 재세요.** 씨앗 "태풍 대비 행동요령"의 후보 최대는
10회인데 "태풍"은 767만회였습니다.

---

## 4. 썸네일을 CDN 에서 뺀다

### 근거 (실측 2026-08-23)

```
PNG 420장 · 84MB
  tc-*.png (썸네일)  111장 · 평균 502KB · 54MB   ← 아무도 안 불러감
  나머지 (옛 도식)   309장 · 평균  98KB · 30MB   ← 발행된 글이 315건 참조

본문이 CDN 이미지를 부르는 횟수
  신규 형식 : 0건   ← 도식을 인라인 CSS 로 바꿔서
  옛 형식   : 315건

월 90편 = 44MB/월 → 12개월 529MB → GitHub Pages 1GB 한도에 2년이면 닿음
```

썸네일은 티스토리에 직접 업로드합니다. 대표이미지는 티스토리가 자기 서버 이미지만
후보로 잡기 때문입니다. `art/*.html` + `tools/tc_*.json` 이 있으면 언제든 재생성됩니다.

### ★ 예외 4장 — 절대 무시하면 안 됨

옛 원고가 CDN 주소로 직접 참조합니다. 빼면 발행된 글의 이미지가 깨집니다.

| 슬러그 | 참조하는 원고 |
|---|---|
| `tc-aircon` | `2026-08-12-aircon-electricity.txt` |
| `tc-beolcho` | `2026-08-12-beolcho-guide.txt` |
| `tc-ansangho` | `2026-08-13-ansangho.txt` |
| `tc-charye` | `2026-08-13-charye-table.txt` |

전체 `tc-*` 중 CDN 참조는 **이 4장이 전부**입니다 (재확인 완료).

### 실행

```gitignore
# 썸네일 PNG 는 티스토리에 직접 업로드하므로 CDN 서빙 대상이 아니다.
# art/*.html + tools/tc_*.json 으로 언제든 재생성된다 (mk-tc.cjs → make-art.cjs).
2026/*/tc-*.png

# 예외 — 옛 원고가 CDN 주소로 직접 부르는 4장
!2026/*/tc-aircon.png
!2026/*/tc-beolcho.png
!2026/*/tc-ansangho.png
!2026/*/tc-charye.png
```

```bash
git rm --cached 2026/*/tc-*.png
git ls-files 2026/ | grep -E 'tc-(aircon|beolcho|ansangho|charye)\.png'
# 4줄이 나와야 정상
```

---

## 5. 굽는 크기를 절반으로

`make-art.cjs:38` 의 `deviceScaleFactor: 2` → `1`.

1600×1600 이 800×800 이 되고 **502KB → 약 130KB** 입니다.
티스토리 목록 썸네일은 300px 남짓이라 800 이면 충분합니다.

**주의**: 옛 도식(`b02-trend.png` 같은)은 본문 폭 860~1100px 로 들어가므로 2배가 필요합니다.
도식은 이제 안 만들지만, 만들 일이 생기면 스케일을 되돌리거나 썸네일 전용 옵션을 둡니다.

---

## 6. `.git` 정리

```bash
git count-objects -vH
git gc --prune=now
```

실측 당시 `size-pack` 36MB 인데 디렉터리가 184MB 였습니다.
지운 9~11월 PNG 273장은 히스토리에 남아 있어 완전히는 안 빠집니다.

---

## 7. 프롬프트에 썸네일 정책 명시

`C:\Users\park-\OneDrive\바탕 화면\블로그작성프롬프트.txt` 의
`## 📅 1. 발행 일정 및 자산 이원화 저장 구조` 밑에 넣습니다.

```
### 썸네일 PNG 는 CDN 서빙 대상이 아닙니다

티스토리 대표이미지는 티스토리에 직접 업로드한 이미지만 후보로 잡습니다.
그래서 tc-*.png 는 다운로드 폴더로만 가고 CDN 에 올릴 이유가 없습니다.

2026/*/tc-*.png 는 .gitignore 대상이라 커밋되지 않습니다. 정상입니다.
"PNG 가 커밋 안 됐네" 하고 억지로 git add 하지 마세요.
```

---

## 8. 10월 시작

`posts/` 에 183편(8월 93 + 9월 90)이 들어간 뒤 시작합니다.

```
- 대상 연월: 2026년 10월
- 폴더명 자동 매칭: 블로그원고_202610
- 하루 3편 × 31 = 총 93편
```

라인업 짤 때 `tools/candidates_202610.md` 를 상위 슬롯에 먼저 배치합니다.

**한 번에 한 달만 돌립니다.** 9~11월을 병렬로 띄웠다가 273편 중 슬러그 59개가 겹쳐
라인업을 통째로 버린 적이 있습니다.

---

## 9. 나중 — 미끼용 / 수익용 레이아웃 분리

87편을 유입 × 수익으로 갈랐더니 이렇게 나왔습니다.

```
유입 O · 수익 O    37편
유입 O · 수익 X    48편   ← 미끼 트래픽. 발행 가치 있음
유입 X · 수익 X     2편   ← 실제로는 1편 (KRX 는 제목만 고치면 삼)
```

미끼 글에 광고를 10개 붙여도 안 눌립니다.

```
태풍        767만 검색   CTR 0.01%   광고 1개
국가장학금   13.9만 검색   CTR 0.00%   광고 0개
```

**미끼 글은 광고를 줄이고 수익 글로 가는 내부 링크를 늘리는 게 맞습니다.**
레이아웃을 두 종류로 나누는 게 다음 개선점입니다.

---

## 도구 사용법 요약

```bash
node tools/advol.cjs <키워드…>                      검색량·CTR·광고수 조회
node tools/advol.cjs --file tools/kw_YYYYMM.json    kw 파일 전체
node tools/kw.cjs --lineup <파일> --out tools/kw_YYYYMM.json
node audit.cjs YYYYMM --full
node tools/artcheck.cjs --posts YYYYMM
node tools/sync.cjs YYYYMM [--all|--check]
node tools/deck.cjs YYYYMM
```

### 검색량 데이터를 다룰 때 반드시 기억할 것

```
compIdx 는 광고주 경쟁률이지 SEO 경쟁률이 아니다
   태풍 767만 검색 · compIdx 중간 · 광고 1개. 그래도 블로그는 1등 못 한다

"< 10" 은 API 가 가린 값일 수 있다
   기초연금 15회 · 자녀장려금 10회로 나온다. 근로장려금은 342,100 으로 정상
   이 한 지표로 주제를 버리지 말 것

검색량 1위가 정답이 아니다
   장애인 활동지원사 39,950 = 구직자
   장애인 활동지원   19,680 = 서비스 수급자

CTR 은 총클릭 / 총검색이다
   PC CTR + 모바일 CTR 을 더하면 이중계산 (자동차보험이 13% 로 나왔다. 실제 5.77%)
```
